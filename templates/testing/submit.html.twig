{% extends 'base.html.twig' %}

{% block title %}Hello TestingController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Testing Test</h1>

<div>
    {{form_start(testingForm)}}
    {# {{form_widget(testingForm)}} #}

    {# Nous pouvons appeler un élément précis du Form avec form_row() #}
    {{form_row(testingForm.Enregistrer)}}

    <p>Ici dans mon Form "Nom" n'est pas un bouton, j'ai simplement modifié son style en appliquant une classe Bootstrap dans le twig</p>
    {# Dans l'ordre que l'on souhaite et le style que l'on modifie ici #}
    {{form_row(testingForm.nom, { 'attr': {'class': 'btn btn-danger'} })}}

    {# Nous pouvons appeler un élément précis du Form avec form_row() #}
    {{form_row(testingForm.Testing)}}
    {# {{form_end(testingForm)}} #}
</div>

</div>
{% endblock %}
